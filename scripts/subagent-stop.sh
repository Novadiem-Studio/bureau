#!/usr/bin/env bash
# Retired under FR4=A REPLACE — post-hoc aggregation via
# scripts/aggregate-transcripts.sh is the sole per-leg token source; stub kept
# permanently so stale hook wiring fails soft.

exit 0
