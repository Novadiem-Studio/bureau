{
  "runtime": "claude",
  "description": "Claude Code adapter — haiku, sonnet, opus; fable for escalation tiers only.",
  "spawn_models": [
    "haiku",
    "sonnet",
    "opus",
    "fable"
  ],
  "forbidden_models": [],
  "capabilities": {
    "supports_fresh_context_subagents": true,
    "supports_parallel_subagents": true,
    "supports_filesystem_tools": true,
    "supports_browser_tools": false,
    "supports_reasoning_effort": false
  },
  "tiers": {
    "cheap": {
      "model": "haiku",
      "notes": "Trivial read-only utility work — Scoot's tier. Split out from sonnet so cheap is a genuinely cheaper rung (haiku/sonnet/opus ladder)."
    },
    "standard": {
      "model": "sonnet",
      "notes": "Analyst, Cleric, Spellwright, Counselor, Mechanic first pass."
    },
    "strong": {
      "model": "opus",
      "notes": "Architecture, critique, frontend/backend first pass."
    },
    "frontier": {
      "model": "fable",
      "notes": "Fable re-enabled for escalation (Jul 2026). Final gates, subtle state/design, high-risk reviews. No effort parameter on this runtime: request extended thinking in the spawn prompt (e.g. think hard) for gate reviews."
    },
    "escalated": {
      "model": "fable",
      "notes": "Fable — repeated failure, hard adjudication, human-requested escalation. No effort parameter on this runtime: the conductor MUST raise effort via the spawn prompt (ultrathink / extended-thinking directive) and allow a larger revision budget; that is the highest-reasoning mechanism on Claude."
    }
  }
}
