# Battle-test: build-review-cold

## Run 2026-07-09

| Case name | Input description | Expected outcome | Actual result |
|-----------|------------------|-----------------|---------------|
| EDGE: Visible-bug only (NOT fire) | A diff that fixes a broken UI component — wrong label text displayed on the dashboard. No DB writes, no money logic, no auth changes, no external sends, no shared-mutable-state RMW. The bug would announce itself immediately on use (wrong text visible). | Self-gate: S1–S5 all score NO. 0 surfaces fire. Route: ship without cold review. | pass — none of S1–S5 fire; the bug is self-announcing (visible wrong text). Gate routes to step 3 (ship). |
| FAILURE MODE: Silent ledger miscount — S2 (MUST fire) | A diff that changes the credit-balance calculation in a billing module: an off-by-one in a running total that produces the wrong balance with no crash, no error, and no visible wrong number on the page (the number renders, just incorrectly). No migration, no auth change, no external send. | Self-gate: S2 (Money/value) fires. Exactly 1 surface fires. Route: run ONE cold build-diff Challenger (step 4). | pass — S2 fires (silent miscount, no crash); 1 surface total. Gate routes to step 4 (cold review). |
| EDGE: S5 plain-async NOT-fire (over-fire guard) | A diff that adds a `setTimeout`-based UI polling loop and a local `await fetch()` to refresh a public dashboard widget. No durable DB write, no ledger/money surface, no auth gate, no external side-effecting send, no shared-mutable-state read-modify-write. The async is local and stateless. | Self-gate: S5 does NOT fire (no durable/shared/money surface, no RMW — plain async with no race failure mode). 0 surfaces fire. Route: ship without cold review. | pass — S5 does not fire; plain async with no durable or money surface is not a concurrency/race in the S5 sense. Gate routes to step 3 (ship). |
| FAILURE MODE: Multi-surface migration + reprice — ESCALATE | A diff that (1) adds a DB migration renaming a ledger column (S1 — durable state, schema change) AND (2) updates the pricing engine to recompute balances using the new column (S2 — money/value, silent miscount risk). Two surfaces fire. | Self-gate: S1 and S2 both fire. ≥2 surfaces fire. Route: ESCALATE to fuller review (execute-plan or feature) — do NOT run the single cold review. | pass — S1 and S2 both fire (2 surfaces); gate routes to escalation, not step 4. The single cold reviewer is under-powered for interacting failure modes across two surfaces. |
