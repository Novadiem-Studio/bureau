{
  "_comment": "templates/accounting.json — canonical schema + reference template for Bundle 04 run accounting (Bundle 11 adds the token/metric sections). Uses JSON-with-_comment-keys format (NOT JSONC; jq cannot parse // comments). Every observation leaf is {value, confidence} — five legal confidence values: exact | estimated | inferred | unavailable | partial. partial: the sum of the shares that are present, with pending shares named in a sibling _note — legal wherever a total can be honestly incomplete (Bundle 11, FR 10). Exempt items carry no confidence: schema_version (structural integer); _comment/_note strings (documentation); container objects and arrays (run, specialist_spawns, phases, cost, usage_snapshot, memory, tokens, conductor_tokens, wall_clock, checkpoints). Emitted accounting.json from a real run carries only data keys plus any _note the script writes — it does NOT reproduce these teaching comments.",

  "schema_version": 2,
  "_comment_schema_version": "schema_version is a structural integer constant. It is the FIRST item on the observation-leaf exemption list — it carries no confidence key. v1 = 1. v2 = 2 (Bundle 11): adds the top-level tokens / conductor_tokens / wall_clock / checkpoints sections and enriches each specialist_spawns[] entry with at / started_at / duration_s / turns / tokens / rework. A run merges to schema_version 2 only when account-tokens.sh returns Bundle 11 token data; a legacy-only re-run (no Bundle 11 event lines in log.md) stays at schema_version 1 with those sections absent.",

  "run": {
    "_comment": "Container — exempt from confidence. Wraps run-metadata leaves.",
    "slug": {
      "value": "20260620-run-accounting-and-resume-signals",
      "confidence": "exact",
      "_comment": "The RUN_DIR basename. Confidence: exact — read directly from the argument."
    },
    "workflow": {
      "value": "feature",
      "confidence": "exact",
      "_comment": "From state.json#workflow. confidence: exact if set; inferred if derived from plan.md or context; unavailable if absent."
    },
    "run_date": {
      "value": "20260620",
      "confidence": "exact",
      "_comment": "Calendar date extracted from the first 8 characters of the RUN_DIR basename, validated in TWO STAGES before any label is assigned. Stage 1: format — the 8 chars must match ^[0-9]{8}$. Stage 2: calendar reality — split as YYYY/MM/DD; YYYY must be 0001-9999 (civil Gregorian, no year zero), MM must be 01-12, DD must be 01 through the actual number of days in that month+year (standard month-length table, leap year = divisible by 4 except century years not divisible by 400). Both stages must pass for confidence:exact. If either stage fails: {value:null, confidence:unavailable, _note: 'RUN_DIR basename does not begin with a valid YYYYMMDD calendar date — run_date cannot be determined'}. Impossible-date examples that MUST degrade: 20261301 (month 13), 20260230 (Feb 30), 00001201 (year 0000 — year zero does not exist in civil Gregorian). Valid: 20240229 (leap year). Invalid: 20230229 (not a leap year). Source is NEVER state.json#last_updated (last mutation timestamp, not run start) and NEVER the RUN_DIR mtime (it moves when accounting.json itself is written)."
    },
    "started_at": {
      "value": null,
      "confidence": "unavailable",
      "_note": "no start-time source in scope for v1; state.json#last_updated is last mutation, not run start",
      "_comment": "v1 has no in-scope source for the precise run start time. state.json#last_updated is the last state-mutation timestamp — NOT a start time. RUN_DIR mtime shifts when accounting.json is written. Both are rejected as proxies. started_at is honestly unavailable."
    },
    "accounted_at": {
      "value": "2026-06-20T14:30:00Z",
      "confidence": "exact",
      "_comment": "UTC ISO-8601 timestamp of when account-run.sh ran. This is the one exact timestamp the script can record unambiguously."
    }
  },

  "specialist_spawns": [
    {
      "_comment": "Array — container, exempt from confidence. One entry per status=started attempt_id in log.md SPAWN-EVENT: records. The Conductor is EXCLUDED (FR 3b-addendum — the Conductor runs in the main session and is never spawned). Configured-but-never-spawned roles are EXCLUDED entirely (no unavailable placeholder row). attempt_id is the SPAWN-EVENT line's dedup/pairing key, built as role+'-'+attempt_number — it is NOT emitted as a leaf in the output (it is the key used to pair started/terminal events during parsing). Entries come from log.md SPAWN-EVENT: lines, NOT from model-routing.json membership.",
      "role": {
        "value": "analyst",
        "confidence": "exact"
      },
      "agent": {
        "value": "Analizer 2000",
        "confidence": "exact"
      },
      "configured_model": {
        "value": "sonnet",
        "confidence": "exact",
        "_comment": "From model-routing.json for this role. Confidence ladder: model-routing.json → exact; model-tiers.json → inferred; neither → unavailable. This answers SHOULD USE, not DID USE."
      },
      "actual_model": {
        "value": "sonnet",
        "confidence": "exact",
        "_comment": "From the SPAWN-EVENT line's actual_model value. confidence:exact when a non-null string token is present. {value:null, confidence:unavailable} when the key is present but its value is JSON null (Conductor did not record the model). A missing actual_model key makes the event invalid at validation — it never reaches this label."
      },
      "attempt": {
        "value": 1,
        "confidence": "exact"
      },
      "reported_status": {
        "value": "complete",
        "confidence": "exact",
        "_comment": "Terminal status from the SPAWN-EVENT termination line (same attempt_id). Legal terminal values: complete | no-handoff | failed | terminated. A spawn with a status:started line but no termination line gets reported_status:started (in-progress or abandoned — surfaced honestly, not hidden). The no-handoff/terminated case is NOT dropped."
      },
      "_comment_bundle11_enrichment": "The six fields below are Bundle 11 (schema_version 2) enrichment, merged into this entry by account-run.sh Step A2(b) from account-tokens.sh's spawn_tokens map. The pairing key is the composite role+'-'+attempt (matching the deterministic attempt_id), NOT attempt_id (which is not a leaf here). A started spawn with no matched SPAWN-TOKEN-EVENT (mid-run or unmatched) keeps its rework flag but omits the token/duration/turns fields. All six are ABSENT at schema_version 1.",
      "rework": false,
      "_comment_rework": "Plain boolean (NOT a {value,confidence} leaf) — carried through from the SPAWN-EVENT started line's optional rework flag (default false). true ONLY when this spawn redoes a deliverable an earlier spawn already attempted (a Challenger-blocker re-spawn, a retried failed/no-handoff spawn, a corrected-design re-spawn). NEVER set on a role's first build of a deliverable, even on its Nth spawn of the run. Drives tokens.rework_ratio.",
      "at": {
        "value": "2026-07-05T00:01:00Z",
        "confidence": "exact",
        "_comment": "Terminal-line timestamp (%Y-%m-%dT%H:%M:%SZ). exact when the spawn's terminal SPAWN-EVENT carried an at; falls back to the started at when no terminal exists."
      },
      "started_at": {
        "value": "2026-07-05T00:00:00Z",
        "confidence": "exact",
        "_comment": "Started-line timestamp, carried forward onto the terminal line by the Conductor."
      },
      "duration_s": {
        "value": 60,
        "confidence": "exact",
        "_comment": "(terminal.at - started.at) via jq fromdateiso8601. exact when both timestamps parse; {value:null, confidence:unavailable} when the spawn is in-progress or a timestamp is malformed. A <=0 value is emitted as-is with a sibling _note (clock skew), never corrected or discarded."
      },
      "turns": {
        "value": 5,
        "confidence": "exact",
        "_comment": "Assistant turn count from the matched SPAWN-TOKEN-EVENT (deduped-message groups carrying a tool_use block)."
      },
      "tokens": {
        "_comment": "Per-spawn token totals from the matched SPAWN-TOKEN-EVENT (deduped by agent_id, take-max). processed = input + cache_creation + cache_read. output is confidence:estimated (large file writes bill as cache_creation, not output); the other four are exact.",
        "input": { "value": 200, "confidence": "exact" },
        "cache_creation": { "value": 300, "confidence": "exact" },
        "cache_read": { "value": 500, "confidence": "exact" },
        "processed": { "value": 1000, "confidence": "exact" },
        "output": { "value": 50, "confidence": "estimated" }
      }
    }
  ],
  "_specialist_spawns_note_example": "When specialist_spawns is [], this sibling _note is present: 'no structured SPAWN-EVENT: lines found in log.md — run may predate the SPAWN-EVENT convention or log was not written'. This distinguishes no-events-found from a script failure.",

  "phases": {
    "_comment": "Container — exempt from confidence. Wraps phase-summary leaves. Sources: state.json#phases_complete, state.json#phase_status, state.json#critic_loops.",
    "complete": {
      "value": ["analyst", "architect", "challenger-r1", "spellwright"],
      "confidence": "exact",
      "_comment": "Copied verbatim from state.json#phases_complete. Confidence is about provenance, not truth: exact means the script read this from state.json faithfully."
    },
    "status": {
      "value": "complete",
      "confidence": "exact",
      "_comment": "From state.json#phase_status. Surfaces EC 5 (in-progress) when the script runs on an incomplete run."
    },
    "critic_loops": {
      "value": { "analyst": 0, "architect": 1, "prompts": 0 },
      "confidence": "exact",
      "_comment": "Copied verbatim from state.json#critic_loops. Work-shape evidence: how many Challenger loops each phase required."
    }
  },

  "cost": {
    "_comment": "Container — exempt from confidence. Cost EVIDENCE, not a cost. v1 ships work-shape signals (specialist count, critic-loop counts, phases completed) as the cost evidence. No dollar or token figure is available from in-scope sources.",
    "currency_estimate": {
      "value": null,
      "confidence": "unavailable",
      "_note": "no billing source in scope for v1",
      "_comment": "PERMANENTLY null/unavailable in v1. No external API, no billing endpoint, no token counts in the usage snapshot. This field is NEVER populated in v1. The _note travels into emitted accounting.json."
    },
    "quota_gauge": {
      "value": {
        "sessionUsedPercent": 42,
        "weeklyUsedPercent": 18,
        "sonnetUsedPercent": 31
      },
      "confidence": "estimated",
      "_comment": "Non-attributable whole-account quota reading from ~/.novadiem/usage-snapshot.json at close-out. NOT a per-run cost attribution. confidence:estimated when snapshot is present, polledAt is parseable, age <= USAGE_SNAPSHOT_MAX_AGE_SECONDS (600s/10min), AND ok:true. confidence:unavailable when snapshot is absent, stale (age > 600s), polledAt malformed/null, or ok:false. SOURCE PATH: the percent fields are nested under the .claude key in usage-snapshot.json (top-level keys are: polledAt, source, ok, providersRequested, providers, claude). Read as .claude.sessionUsedPercent, .claude.weeklyUsedPercent, .claude.sonnetUsedPercent — NOT .sessionUsedPercent at the top level."
    }
  },

  "usage_snapshot": {
    "_comment": "Container — exempt from confidence. Describes the ~/.novadiem/usage-snapshot.json read at accounting time.",
    "present": {
      "value": true,
      "confidence": "exact",
      "_comment": "Is there a USABLE snapshot? true when the file is present AND parses as a JSON object; false when absent OR present-but-unusable (unparseable, or valid JSON but not an object). The absent-vs-corrupt distinction is carried in the sibling polled_at/age_seconds/stale _note, not here. confidence: exact — always observable."
    },
    "polled_at": {
      "value": "2026-06-20T14:20:00Z",
      "confidence": "exact",
      "_comment": "snapshot#polledAt when present and parseable. {value:null, confidence:unavailable} when snapshot is absent OR polledAt is null/missing/not-parseable-ISO-8601."
    },
    "age_seconds": {
      "value": 600,
      "confidence": "exact",
      "_comment": "now - polledAt, computed by jq (NOT date -d or date -j — those diverge between macOS and Linux). jq expression: now - (.polledAt | fromdateiso8601) | floor. {value:null, confidence:unavailable, _note} when polledAt is absent or malformed. When snapshot is absent: {value:null, confidence:unavailable}."
    },
    "stale": {
      "value": false,
      "confidence": "exact",
      "_comment": "age_seconds > USAGE_SNAPSHOT_MAX_AGE_SECONDS (600) OR ok:false → stale=true. confidence:exact when a snapshot is present and polledAt is parseable. IMPORTANT: when NO snapshot exists, stale is {value:null, confidence:unavailable} — NOT {value:false, confidence:exact}. You cannot observe staleness of a snapshot that is not there. Same null/unavailable treatment applies to polled_at and age_seconds in the absent case."
    }
  },

  "memory": {
    "_comment": "Container — exempt from confidence. CONDITIONAL: this block is OMITTED ENTIRELY from real-run accounting.json when state.json has NO top-level 'memory' key (FR 9 / EC 8). Its presence here in the template teaches the field shape. In real runs, the 'memory' key's presence and shape in state.json is the single switch: (1) no memory key → omit block; (2) memory == {} empty object → emit block with all six fields {null, unavailable}; (3) memory is a non-object (string/number/bool/array/null) → emit block with all six fields {null, unavailable} + sibling _note; (4) memory is a non-empty object → validate each sub-field independently. The Conductor writes state.json#memory during the run if Rheo/MOT memory was consulted; the script reads it, never writes it.",
    "retrieval_count": {
      "value": 3,
      "confidence": "exact",
      "_comment": "Number of memory lookups. exact when an integer >= 0; {null, unavailable, _note} when absent or wrong type."
    },
    "writes_proposed": {
      "value": 1,
      "confidence": "exact"
    },
    "writes_accepted": {
      "value": 1,
      "confidence": "exact"
    },
    "conflicts_flagged": {
      "value": 0,
      "confidence": "exact"
    },
    "digest_freshness": {
      "value": "PT2H",
      "confidence": "exact",
      "_comment": "ISO-8601 duration or a staleness label. exact when a string; {null, unavailable, _note} when absent or wrong type."
    },
    "memory_preflight_passed": {
      "value": true,
      "confidence": "exact",
      "_comment": "boolean. exact when present and boolean; {null, unavailable, _note} when absent or wrong type."
    }
  },

  "tokens": {
    "_comment": "Container — exempt from confidence. Bundle 11 (schema_version 2) ONLY: present when account-run.sh merged account-tokens.sh output. Derived from log.md SPAWN-TOKEN-EVENT (deduped by agent_id, take-max) + CONDUCTOR-TOKEN-EVENT (deduped by session_id take-max, summed across legs). ABSENT at schema_version 1.",
    "processed_total": {
      "value": 2000,
      "confidence": "exact",
      "_semantics": "processed sums per-turn cumulative usage (input + cache_creation + cache_read) across deduped messages — a billing-shaped figure that scales with turn count, not a unique-token count; use for before/after comparisons only",
      "_comment": "Specialist processed (deduped per agent_id) + conductor processed (deduped per session_id, summed). confidence:exact ONLY when (a) >=1 CONDUCTOR-TOKEN-EVENT has final:true AND (b) every terminal SPAWN-EVENT has a matched SPAWN-TOKEN-EVENT. Otherwise confidence:partial with a sibling _note naming the pending share(s): 'conductor-share-pending: final-leg capture not yet in log.md' and/or '<n> specialist spawn(s) have no matched SPAWN-TOKEN-EVENT'."
    },
    "output_total": {
      "value": 95,
      "confidence": "estimated",
      "_comment": "Specialist output + conductor output. ALWAYS confidence:estimated — large file writes bill as cache_creation, not output (EC 3 / AC 9)."
    },
    "rework_ratio": {
      "value": 0.15,
      "confidence": "exact",
      "_comment": "(sum of processed over spawns flagged rework:true) / processed_total. confidence tracks processed_total (a derived metric is never more confident than its inputs). 0.0 when no spawn is rework:true; {null, unavailable} when processed_total is 0."
    },
    "tokens_per_loop": {
      "value": 1000.0,
      "confidence": "exact",
      "_comment": "processed_total / sum(state.json#critic_loops). confidence tracks processed_total. {null, unavailable} when total critic_loops is 0 (never divide by zero)."
    },
    "unattributed_records": {
      "value": [],
      "confidence": "exact",
      "_comment": "Array of SPAWN-TOKEN-EVENT payloads whose attempt_id is null OR has no matching SPAWN-EVENT started line — surfaced, never dropped, and still counted into processed_total. This is a container leaf; the value carries the array."
    }
  },

  "conductor_tokens": {
    "_comment": "Container — exempt from confidence, EXCEPT it carries a block-level confidence key (below). Bundle 11 (schema_version 2) ONLY. The Conductor runs in the main session and is never a specialist_spawns[] entry (AC 10) — its token usage lives here, distinct. Multi-leg: dedup per session_id take-max, then sum across distinct session_ids (resumed runs have >1 leg).",
    "tokens": {
      "input": 40,
      "cache_creation": 60,
      "cache_read": 100,
      "processed": 200,
      "output": 10,
      "_comment": "Summed conductor totals across deduped legs. processed = input + cache_creation + cache_read."
    },
    "turns": 42,
    "legs": 1,
    "confidence": "exact",
    "_comment_confidence": "exact when >=1 CONDUCTOR-TOKEN-EVENT has final:true; partial + _note 'final-leg-capture-pending: post-close-out Stop hook has not yet fired for this run' when conductor lines exist but none is final:true; unavailable + _note when no CONDUCTOR-TOKEN-EVENT lines exist at all."
  },

  "wall_clock": {
    "_comment": "Container — exempt from confidence. Bundle 11 (schema_version 2) ONLY. Derived from paired SPAWN-EVENT started/terminal timestamps.",
    "active_spawn_time_s": {
      "value": 120,
      "confidence": "exact",
      "_semantics": "summed active time of individual spawns — not run wall-clock: parallel spawns sum to more than the wall-clock they overlapped in, and human-wait between spawns is excluded",
      "_comment": "Sum of duration_s over all spawns where both timestamps parse. confidence:exact when every paired spawn's duration_s is exact; partial when any spawn's duration_s is unavailable."
    },
    "minutes_per_loop": {
      "value": 1.0,
      "confidence": "exact",
      "_comment": "active_spawn_time_s / 60 / sum(critic_loops). confidence tracks active_spawn_time_s. {null, unavailable} when total critic_loops is 0."
    }
  },

  "checkpoints": {
    "_comment": "Container — exempt from confidence. Bundle 11 (schema_version 2) ONLY. From CHECKPOINT-EVENT raised/resolved pairs in log.md.",
    "entries": [
      {
        "_comment": "One entry per raised checkpoint. decision is null when absent from the resolved line. wait_s.confidence:exact when the checkpoint resolved; unavailable when raised-only (EC 5). A wait_s <= 0 is emitted as-is with a sibling _note (EC 10), never corrected or discarded.",
        "id": "design-model",
        "raised_at": "2026-07-05T00:02:00Z",
        "resolved_at": "2026-07-05T00:04:00Z",
        "decision": "proceed as designed",
        "wait_s": { "value": 120, "confidence": "exact" }
      }
    ],
    "human_wait_total_s": {
      "value": 120,
      "confidence": "exact",
      "_comment": "Sum of wait_s over entries whose wait_s.confidence is exact (resolved pairs only). confidence:exact when every raised checkpoint has a resolved pair; partial + _note when any raised-only entries exist. Raised-only checkpoints never contribute."
    },
    "active_vs_blocked_ratio": {
      "value": 0.4,
      "confidence": "exact",
      "_comment": "wall_clock.active_spawn_time_s / human_wait_total_s. confidence is the less-confident of the two inputs. {null, unavailable} when human_wait_total_s is 0 or null."
    }
  }
}
