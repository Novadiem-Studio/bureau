# Bundle 24 — full-corpus zero-FP evidence rule (row 15 + Challenger obligation)

## Run 2026-07-09

Row 15 (`agents/architect.md § Pre-handoff self-check` conditional-checks table) and
the Hard-gate zero-FP evidence gate bullet (`agents/critic/spec-plan.md`) are both
ADVISORY prose edits with no script backstop. Cases use `slow: human judgment required`
because the trigger is a producer read of the two-signal AND conjunction, not a script
call. The make-or-break case is (c): a false fire on an advisory-only or mere-mention
spec would make the row gate-theater. Evaluated by The Mechanic against the worktree
canon at commit 4100398.

| Case name | Input description | Expected outcome | Actual result |
|-----------|------------------|-----------------|---------------|
| Edge case — subset-only claim caught (AC7a) | A spec introduces a new hard-gate preflight check (`check_zero_fp`) with an auto-reject verb (`auto-rejects`) bound to an introduction verb (`We add`), Signal 1 fires. The spec claims zero false positives but evidences only a 7-spec subset, not the pinned full corpus. Sample text: "We add a new `check_zero_fp` preflight function that auto-rejects any spec claiming zero false positives without evidence. Tested against 7 specs — 0 false positives." | Row 15 fires. Both signals present: Signal 1 (`add` + `check_zero_fp`), Signal 2 (`0 false positives` bound to the new gate). Evidence form defect: `subset-only — 7 specs measured, full corpus is <M>`. Challenger obligation blocks as Blocker the producer closes. | pass — human judgment: Signal 1 fires on `We add a new \`check_zero_fp\`` (new `check_*` name + introduction verb `add`); Signal 2 fires on `0 false positives` bound BY READING to that same gate. Evidence contract missing pinned corpus + K>0: defect is `subset-only — 7 specs measured, full corpus is <M>` (verbatim from row 15 Evidence form). Challenger obligation demands full-corpus pinned evidence as Blocker. slow: human judgment required |
| Failure mode — 0-triggers claim caught (AC7b) | A spec introduces a new merge gate (`block_missing_section`) that auto-rejects plans with no Architecture section, Signal 1 fires. The spec states "Full-corpus run: 0 fires — zero false positives." K=0 (the grammar triggered on nothing). Sample text: "We introduce a new `block_missing_section` merge gate that auto-rejects plans with no Architecture section. Full-corpus run: 0 fires — zero false positives." | Row 15 fires. Both signals present: Signal 1 (`introduce` + `block_missing_section` + `auto-rejects`), Signal 2 (`zero false positives` + `0 fires` bound to the new gate). K=0 is unexercised — the grammar never triggered, so zero-FP is not proven. Evidence form defect: `unexercised — 0 triggers on <M> specs`. Blocker. | pass — human judgment: Signal 1 fires on `We introduce a new \`block_missing_section\`` (introduction verb `introduce` + auto-reject verb `auto-rejects` in same clause); Signal 2 fires on `0 fires — zero false positives` bound BY READING to that same gate. K=0: the grammar triggered on zero specs, so the zero-FP claim is vacuous — evidence form defect is `unexercised — 0 triggers on <M> specs` (verbatim from row 15). Challenger obligation: Blocker the producer closes, not a full-corpus re-run by the Challenger. slow: human judgment required |
| Negative / gate-theater guard (AC7c) | Two sub-cases, both must NOT fire. (c1) Advisory-only zero-FP claim: a spec row says "this advisory self-check claims zero false positives on the corpus." Signal 2 is present but Signal 1 is ABSENT — no new auto-reject mechanism introduced. (c2) Mere-mention of an existing gate: "The existing Check-h convention-citation gate blocks round1 for any spec that omits a citation. It has recorded zero false positives on 45 specs." Signal 1 CANDIDATE fires on `hard gate` and `blocks` but no introduction verb is present (`add`/`introduce`/`new`/`promote`). | Row 15 is SILENT on both sub-cases. The two-signal AND is not satisfied: (c1) Signal 1 absent (no new auto-reject mechanism); (c2) Signal 1 absent (auto-reject verb present but no introduction verb binds it to a new gate). No defect emitted, no Blocker. Advisory/self-check claims and mere-mentions of existing gates are explicitly out of scope. | pass — human judgment: (c1) Signal 1 does not fire — the spec row describes an advisory self-check with no auto-reject mechanism (`check_*` or auto-reject+intro verb pair); Signal 2 present but AND not satisfied → row silent; (c2) Signal 1 does not fire — `blocks round1` and `hard gate` are present but there is NO introduction verb (`add`/`introduce`/`new`/`promote … to`) bound to them; the gate is described as EXISTING (`The existing Check-h ...`), not introduced; AND not satisfied → row silent. Both sub-cases: no fire. This is the gate-theater guard that proves the row does not fire on every spec mentioning gates or zero-FP. slow: human judgment required |

---

# Bundle 12 — planning-loop reduction

## Run 2026-07-05

Promotion run (full standing matrix per `docs/conductor-gates.md § Re-run-at-promotion`;
FR 8 modifies the analyst trigger the Bundle 03 greenfield-gate cases pin on). Three
sub-sections: Bundle 12 cases, Bundle 03 re-run, Bundle 05 re-run. Evaluated by the
Conductor against the worktree canon at commit 4806c6d.

### Bundle 12 cases

| Case name | Input description | Expected outcome | Actual result |
|-----------|-------------------|-----------------|---------------|
| Happy path — self-observing spec with reconciliation section | A spec whose FRs describe parsing `log.md` headings and `SPAWN-EVENT` lines, carrying an `## Observed-behavior reconciliation` section citing 2 real run logs. Challenger round 1 reviews. | Observed-behavior-absence gate (FR 4) does NOT fire (condition 2 false). | pass — gate fires only when BOTH conditions hold (`agents/critic/spec-plan.md:142-150`); section present → no fire. Live instance: this run's own spec.md (parses framework artifacts, carries the section with 2 cited logs + 7 observations). |
| Failure mode — self-observing spec, section absent | The same spec WITHOUT the `## Observed-behavior reconciliation` section. Challenger round 1 reviews. | FR 4 Observed-behavior-absence gate fires as **Blocker**. | pass — condition 1 true (parsing verbs `parse`/`read`/`grep` applied to `log.md` headings + `SPAWN-EVENT` lines, two of the three named signal anchors), condition 2 true (no section) → Blocker per the gate text; severity stated in the gate block. |
| Edge case — FR 8 trigger symmetry (four sub-cases) | (i) greenfield explicitly declared; (ii) existing-project explicitly declared; (iii) ambiguous — no Mode declaration, no codebase referenced; (iv) CORNER: no Mode declaration, codebase IS referenced. Compare `agents/analyst.md` When-to-include vs `agents/critic/spec-plan.md` FR-11. | (i) both fire; (ii) neither fires; (iii) both fire; (iv) both fire — identical conditions in all four. | pass — producer (`analyst.md:121-126`): include on explicit greenfield OR no-Mode-declaration; codebase-referenced explicitly non-gating; omit only on explicit existing-project. Checker (`spec-plan.md:127-134`): required iff existing-project mode not declared. Both reduce to the same predicate; corner sub-case (iv) both fire — the asymmetry found at the P2+P3 review is closed and pinned here. |
| Edge case — reconciliation with no Architecture section (EC 3) | Reconciliation-mode Analyst spawned on a spec.md with no Architecture section. | Writes `RECONCILED: no Architecture section found in spec.md — reconciliation skipped` to log.md and returns; valid terminal state, not a failure. | pass — exact skip-note text and valid-terminal-state rule at `agents/analyst.md:210-212`; EC 1 clean-note validity stated alongside. |
| Failure mode — Architect proposes cutting an FR (EC 2) | Reconciliation pass finds the Architecture narrows a written FR not yet human-ratified. | Analyst flags it in the `RECONCILED:` note (PENDING-RATIFICATION), does NOT rewrite/delete the FR; decision goes to the design-model checkpoint. | pass — flag-don't-delete rule at `agents/analyst.md § Reconciliation mode` EC 2 block; LIVE evidence from this run: analyst-2 flagged the FR 5b scope narrowing as PENDING-RATIFICATION, left FR text unchanged, and the checkpoint (01) ratified it — the full designed path executed on a real drift. |

### Bundle 03 re-run (against the updated FR 8 trigger)

| Case | Result |
|------|--------|
| Happy path — greenfield with outcome | pass — greenfield declared: producer clause (a) includes the table; FR-11 requires it (no existing-project declaration); FR 4 outcome gate + FR 12 unchanged and present (`spec-plan.md:111`). |
| Edge — explicit exploratory run | pass — existing-project declared → producer omits, FR-11 does not fire; FR 4 exploratory escape unchanged. |
| Edge — existing-project run | pass — "Omit entirely when the Orchestrator explicitly declares existing-project mode" (new producer clause) matches FR-11's declaration-keyed no-fire exactly. |
| Failure — all three gates fire | pass — FR 4 (absent outcome), FR 11 (exact heading absent), FR 12 (load-bearing undercited row) all fire; gate texts untouched by this bundle (P5 added the new gate adjacent; FR-11 verified unchanged at the final build review). |
| Failure — bake-off with no criteria | pass — EC 5 subcondition intact under the FR 4 outcome-metric gate block. |

### Bundle 05 re-run (Notary — untouched by FR 8; completeness per full-matrix obligation)

| Case | Result |
|------|--------|
| Happy path — well-formed cue, matching hashes | pass — `templates/external-review.json` carries allowlist/hashes/provenance (jq-verified); receipt/verdict contract per `docs/notary-review.md` ITEMs intact. |
| Edge — memory excerpt with valid provenance | pass — provenance three-field rule present (ITEM 6). |
| Failure — prohibited-input cold-break | pass — NOTARY FLAG text present in `agents/notary.md` (5 occurrences incl. the log.md cold-break). |
| Failure — hash mismatch | pass — ITEM 5 mismatch-receipt + halt behavior present. |
| Failure — provenance sub-field missing | pass — per-entry flag vs whole-review cold-break distinction (ITEM 6/7) intact. |

---

# Bundle 03 — Planning decision quality gates

## Run 2026-06-20

| Case name | Input description | Expected outcome | Actual result |
|-----------|-------------------|-----------------|---------------|
| Happy path — greenfield with outcome | Greenfield run; Analyst fills `Outcome / bottleneck: <specific failable metric>` + `### Greenfield Assumptions` table with all four status categories represented; Architect closes `needs-Architect` rows in existing subsections; no memory citations; Challenger round 1 reviews. | Challenger passes all three gates (FR 4, FR 11, FR 12) and their EC subconditions; no new blockers. | pass — FR 4: non-trivial outcome field present; FR 11: exact `### Greenfield Assumptions` heading present with all four status categories; FR 12: no memory citations, so citation check does not fire. All three gates clear. |
| Edge case — explicit exploratory run | Existing-project run (Mode: existing project declared in spec header and spawn prompt). Analyst declares `Exploratory run — no outcome metric; reason: evaluating feasibility of X`. No `### Greenfield Assumptions` section present. Challenger reviews spec+plan. | FR 4 gate does NOT block (exploratory escape accepted). FR 11 gate does NOT fire a Blocker (existing-project Mode declared — primary trigger not met). | pass — FR 4: exploratory escape carries a one-sentence reason and the run does not produce spec+plan+prompts (EC 2 does not trigger); gate accepts. FR 11: Challenger keys on explicit Mode declaration ("Mode: existing project" in spec header and spawn prompt); primary trigger not met; no Blocker. |
| Edge case — existing-project run | Existing-project run (Mode: existing project declared in spec header and spawn prompt); Analyst omits `### Greenfield Assumptions` section. Challenger reviews spec+plan. | FR 11 gate does NOT fire a Blocker; Challenger keys on explicit Mode declaration, not Architecture content. | pass — FR 11 explicitly demotes "Architecture proposes technology choices" to corroborating-only signal; the primary trigger is the explicit Mode declaration, which reads existing-project; gate does not fire. |
| Failure mode — all three gates fire | Non-exploratory greenfield spec: (a) `Outcome / bottleneck:` field absent; (b) has a `### Assumptions` section (wrongly named — not `### Greenfield Assumptions`) containing a `decided` row whose memory evidence is missing source, confidence, timestamp, and staleness flag; that undercited assumption is load-bearing to a design choice. Challenger reviews spec+plan. | FR 4 fires as **Blocker** (absent outcome field). FR 11 fires as **Blocker** (exact `### Greenfield Assumptions` heading absent). FR 12 fires as **Blocker** (load-bearing undercited memory row in the misnamed section). All three gates fire at their specified severities. | pass — FR 4: field absent on a non-exploratory run → Blocker. FR 11: section present but heading is `### Assumptions`, not the exact `### Greenfield Assumptions` string required → Blocker (absence of exact heading). FR 12: decided row missing all four citation fields; the undercited assumption is load-bearing to a material design choice → escalates from Warning to Blocker per FR 8. All three fire. |
| Failure mode — bake-off with no criteria | Architect recommends a bake-off in plan.md but does not pre-declare evaluation criteria. All other gates pass. Challenger reviews spec+plan. | EC 5 subcondition inside `Outcome-metric gate (FR 4)` fires as a **Blocker**; FR 11 and FR 12 do not fire. | pass — EC 5 is a labeled subcondition under FR 4 ("EC 5 — bake-off criteria:"); Challenger reads plan steps and finds criteria-less bake-off → Blocker. FR 11: greenfield run with `### Greenfield Assumptions` present → does not fire. FR 12: no undercited memory assumptions → does not fire. Only EC 5 fires. |

---

# Bundle 05 — The Notary

## Run 2026-06-20

| Case name | Input description | Expected outcome | Actual result |
|-----------|-------------------|-----------------|---------------|
| Happy path — well-formed cue, matching hashes | Cue packet has `allowlist` (two file paths), `hashes` (both files sealed with matching SHA-256), no `provenance` entries (no memory excerpts). Conductor passes RUN_DIR + packet path only. Notary reads both files, checks hashes, writes coldness receipt and advisory verdict. | Clean review artifact at `RUN_DIR/reviews/notary-r1.md`; receipt lists both files as read + hash-matched; `Coldness: intact`. No NOTARY FLAG line. | pass — `templates/external-review.json` supplies `allowlist`/`hashes` with correct defaults (verified jq). `docs/notary-review.md` ITEM 3 specifies per-file receipt fields (path, hash supplied, read, deviation). ITEM 4 specifies five required artifact sections (a–e). `agents/notary.md` Outputs names `notary-<request_id>.md`; Handoff footer includes `Coldness:` field. All components in place for the happy path. |
| Edge case — memory excerpt with valid provenance | Cue allowlists a memory excerpt and carries a conforming `provenance` entry with all three required sub-fields (source, confidence, timestamp). Other allowlisted files are plain (no provenance entries). | Notary reads the memory excerpt, records its provenance in the receipt — no flag fired. Remaining files also read normally. `Coldness: intact`. | pass — `templates/external-review.json` has `provenance: {}` field. `docs/notary-review.md` ITEM 6 states: if provenance object is complete (source/confidence/timestamp present), no flag; Notary reads the file and records provenance in the receipt. `agents/notary.md` Inputs declares "hash + provenance verification per docs/notary-review.md." A complete provenance entry does not trigger the per-entry flag. |
| Failure mode — prohibited-input cold-break | Spawn prompt includes `log.md` contents inline (or passes it explicitly alongside the cue). All other cue fields are valid. | `NOTARY FLAG: received log.md — coldness broken, did not review` written to review artifact; Notary stops immediately; no findings; `Coldness: broken`. | pass — `agents/notary.md` Inputs names log.md as denied; Authority section specifies the prohibited-input cold-break with exact NOTARY FLAG text. `docs/notary-review.md` ITEM 7 confirms this is a WHOLE-REVIEW cold-break (not per-entry); Notary writes the flag line and produces no findings. Flag text matches across persona and protocol doc. |
| Failure mode — hash mismatch | Cue supplies a SHA-256 hash for an allowlisted file; the file on disk has since changed (hash no longer matches). Other allowlisted file has no hash in the cue. | Coldness receipt records both the cue hash and actual hash for the mismatched file; `NOTARY FLAG: hash mismatch on <path> — coldness broken, did not review` written; Notary halts; no findings. The unhashed file's read status is not reached. | pass — `docs/notary-review.md` ITEM 5 specifies: record mismatch in receipt (cue hash, actual hash, path), write the NOTARY FLAG line, halt with no findings. Coldness receipt format (ITEM 3) has `Deviation: mismatch (cue hash: <hex>, actual hash: <hex>)` field. ITEM 5 explicitly distinguishes this from EC 3 (file not found = finding, not cold-break). `agents/notary.md` references ITEM 5 behavior via docs/notary-review.md. |
| Failure mode — memory excerpt missing provenance sub-fields | Cue allowlists a memory excerpt and declares it in `provenance`, but the provenance object is missing `timestamp` (source and confidence are present). A second allowlisted file is a plain file with no provenance entry. | Per-entry `NOTARY FLAG: memory excerpt <path> missing provenance (timestamp) — not read`; that entry is skipped and recorded in the receipt as not read; the remaining plain file is read normally; `Coldness: intact` (per-entry flag, not a whole-review cold-break). | pass — `docs/notary-review.md` ITEM 6 specifies per-entry behavior: if any of source/confidence/timestamp is missing, write `NOTARY FLAG: memory excerpt <path> missing provenance (<which fields>) — not read`; skip that entry; continue with remaining allowlisted files. This is explicitly NOT a whole-review cold-break. `agents/notary.md` Authority references docs/notary-review.md for all provenance checks. Per-entry vs. whole-review distinction confirmed consistent across ITEM 6 and ITEM 7. |
