# Evaluation Logs

Public repos should keep detailed evaluation logs out of git.

Use `docs/evaluation/` locally for real audits, review notes, and private quality records. That directory is ignored.
